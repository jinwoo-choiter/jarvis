## 1. Loader and prompt updates

- [x] 1.1 Simplify `jarvis/_config.load_config` to read only `config.yaml`; drop the `config.local.yaml` merge branch and the now-unused `local_path` variable.
- [x] 1.2 Update the module docstring at the top of `jarvis/_config.py` to describe the new single-file model (no layering).
- [x] 1.3 Update `prompts/daily_brief.md` §0 file-list to reference `config.yaml` and `profile.yaml` (replacing `config.local.yaml` and `profile.local.yaml`); remove the `profile.example.yaml` fallback clause.

## 2. Init script

- [x] 2.1 Create `scripts/init.sh` with `#!/usr/bin/env bash` and `set -euo pipefail`.
- [x] 2.2 Implement parsing of `.env.example`: collect `(comment, key)` pairs in declaration order; bail with a diagnostic on any malformed line that is not blank, comment, or `KEY=`.
- [x] 2.3 Implement the existing-`.env` guard: detect, warn, prompt for explicit confirmation, exit zero on decline.
- [x] 2.4 Implement the per-key prompt loop using `read -r` (and `read -s` for any key whose name contains `KEY`, `TOKEN`, `SECRET`, or `PASSWORD`); display the comment as help text above each prompt.
- [x] 2.5 Write the resulting `.env` file with `key=value` lines and a header comment noting it was generated by `scripts/init.sh`.
- [x] 2.6 `chmod 600` on the resulting `.env`.
- [x] 2.7 Implement the optional Slack smoke test: default-yes prompt, single `curl` POST with a `[JARVIS init]`-prefixed message, surface non-2xx as a non-zero exit.
- [x] 2.8 `chmod +x scripts/init.sh`.

## 3. Repository file moves and edits

- [x] 3.1 Replace the contents of `config.yaml` with the maintainer's current `config.local.yaml` content, verbatim. Keep useful section comments from the original `config.yaml` where they still describe the new file's layout.
- [x] 3.2 Create `profile.yaml` from the maintainer's current `profile.local.yaml` content, verbatim.
- [x] 3.3 Delete `config.local.yaml.example` and `profile.example.yaml`.
- [x] 3.4 Edit `.gitignore` to remove `*.local.yaml` and `prompts/*.local.md`. Keep `.env`, `state/seen.sqlite`, `run.log`, `__pycache__/`, `.venv/`, `/tmp/raw/`, `/tmp/briefing.md`.
- [x] 3.5 Update `.env.example` so that every variable line has a single leading `#` comment line describing what the value is and where to obtain it (Slack incoming-webhook URL doc page; YouTube Data API console).

## 4. README

- [x] 4.1 Rewrite the "Setup" section: clone, `bash scripts/init.sh`, edit `config.yaml` and `profile.yaml`, run `bash run.sh`. Remove all references to `config.local.yaml`, `profile.local.yaml`, and template-copy steps.
- [x] 4.2 Add an "If you forked this repo" subsection that explicitly directs the forker to edit `config.yaml` (channel list, search themes, arXiv categories) and `profile.yaml` (role, priority keywords, upcoming events) before their first run.
- [x] 4.3 Verify the README's existing safety section (Push Protection, pre-commit hooks, pre-push checklist) is still accurate after the gitignore changes; adjust wording if it referenced `*.local.*`.

## 5. Smoke test the new flow end to end

- [x] 5.1 In a scratch directory, simulate a fresh-clone setup: `git clone <local repo>`, `bash scripts/init.sh` with throwaway values for SLACK_WEBHOOK_URL (`https://example.invalid/x`) and YOUTUBE_API_KEY (`dummy`), decline the smoke test, verify `.env` is mode 600.
- [x] 5.2 In the same scratch directory, re-run `bash scripts/init.sh`, decline the overwrite prompt, verify `.env` is unchanged.
- [x] 5.3 In the actual repository, with the maintainer's real `.env` already populated from the migrated state, run `bash run.sh` once and verify the resulting brief is identical in shape to a brief produced under the previous (layered) configuration model.

## 6. Maintainer-side cleanup (post-merge)

- [x] 6.1 Delete the now-orphaned `config.local.yaml` and `profile.local.yaml` from the maintainer's working tree.
- [x] 6.2 Confirm `git status` is clean after deletion (the files were gitignored and were never tracked, so deletion produces no staged changes).
